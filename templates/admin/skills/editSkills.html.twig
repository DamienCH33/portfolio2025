{% extends 'admin/layout.html.twig' %}

{% block title %}Modifier la compétence{% endblock %}

{% block body %}
<div class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="m-0">✏️ Modifier la compétence</h2>
        <a href="{{ path('admin_skills') }}" class="btn btn-primary-custom">↩ Retour</a>
    </div>

    {{ form_start(form) }}
    <div class="card p-4 shadow-sm">
        <div class="row">
            <div class="col-md-6 mb-3">
                {{ form_label(form.name) }}
                {{ form_widget(form.name, {'attr': {'class': 'form-control'}}) }}
            </div>
            <div class="col-md-6 mb-3">
                {{ form_label(form.category) }}
                {{ form_widget(form.category, {'attr': {'class': 'form-select'}}) }}
            </div>
            <div class="col-md-12 mb-3">
                {{ form_label(form.logo) }}
                {{ form_widget(form.logo, {'attr': {'class': 'form-control'}}) }}
            </div>
        </div>
        <button type="submit" class="btn btn-primary-custom mt-3">💾 Mettre à jour</button>
    </div>
    {{ form_end(form) }}
</div>
{% endblock %}
